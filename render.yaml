services:
  - type: web
    name: invitatia-mea-nuptiala
    env: static
    buildCommand: npm install && npm run build
    publishPath: dist/invitatie/browser
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
