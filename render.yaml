services:
  - type: web
    name: invitatie
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: dist/invitatie/browser
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
